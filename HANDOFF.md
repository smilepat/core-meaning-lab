# HANDOFF — core-meaning-lab

**작성 2026-08-07 · DESKTOP-JDF6C5D · 기준 커밋 `16ca9bc`(300단어 완주)**

이 문서는 **왜 지금 모양인가**를 담는다. 지금 상태와 다음 할 일은 `STATUS.md`,
실행·구조는 `README.md`, 저장소 규칙은 `CLAUDE.md` 에 있다. 여기 옮겨 적지 않는다.

> ⚠️ 이 문서는 **결정과 함정**을 적은 것이라 코드가 바뀌어도 대부분 유효하다.
> 다만 §5(검증된 것/안 된 것)와 §7(열린 판단)은 시간이 지나면 낡는다.
> 구조·아키텍처를 바꿨으면 §3·§4를, 검증을 더 했으면 §5를 고쳐라.

---

## 1. 이 앱이 걸고 있는 것

**뜻을 여러 개 외우는 대신 core 하나에서 뻗어 나가게 한다.**

이건 UI 취향이 아니라 이 앱의 유일한 주장이다. 그래서 데이터에서 가장 중요한 필드는
단어도 예문도 아니고 **`bridge`** — core 와 그 확장 의미를 잇는 한 문장이다.

```ts
{ key: "operate", label: "운영하다",
  bridge: "가게·조직이 멈추지 않고 계속 굴러가게 하다" }   // ← 이게 알맹이
```

`bridge` 가 부실하면 이 앱은 **그냥 뜻 나열**이 된다. 화면도 채점도 다 멀쩡히 돌지만
존재 이유가 사라진다. 데이터를 늘릴 때 여기서 타협하면 안 된다.

채점 프롬프트도 이 주장 위에 서 있다. 서버는 학생 답이 맞았는지만 보지 않고
**core 에서 그 뜻이 어떻게 나오는지 한 문장 덧붙이도록** 지시한다(`server/grade.ts`).
실제로 이렇게 나온다:

> "'적어서 잡아 두다'라는 핵심 의미가 장부에 이름을 적어 미리 자리를 확보하는
> '예약하다'로 확장된 것입니다." — `book` 오답 채점

---

## 2. 단어를 이렇게 고른 이유

### 1~3군 60개는 정본이다 — 개수를 늘리지 않는다

사용자가 수능·평가원·EBS 빈도를 기준으로 확정한 목록이다. 여기에 단어를 끼워 넣으면
**학습 우선순위 자체가 흔들린다.** 그래서 `tests/catalog.test.ts` 가 각 20개로 못 박고 있고,
확장분은 전부 4·5군에 쌓았다.

### 최우선 30개는 순서가 곧 설계다

`rank` 1~30 (1.run … 30.respond). 앱의 기본 학습 경로이고, 첫 화면이 여기서 시작한다.
순위가 겹치면 카탈로그 검사가 앱 기동 시 예외로 죽인다.

### 5군은 어근 가족으로 짠다 — 이 방식이 실제로 먹혔다

접두사가 뜻을 어떻게 비트는지는 **나란히 놓여야** 보인다. 그래서 한 어근에서
**네 형제씩 대칭**으로 뽑았다.

```text
con·sist  함께 서 있다        per·sist  끝까지 서 있다
re·sist   맞서 버티고 서다     in·sist   위에 딱 버티고 서다
```

가장 값이 컸던 둘:

- **`succeed`** — "성공하다"와 "계승하다"가 왜 한 단어인지 따로 외울 일이 아니다.
  core 가 **뒤따라 가다**다. 뒤를 이으면 계승, 끝까지 따라가 닿으면 성공.
- **`circumstance`** — 번역어 "상황"보다 **둘레에 서 있는 것들**이 정확하다.

⚠️ **닮았다고 다 한 식구가 아니다.** `preserve`·`conserve`·`reserve`·`observe` 는
`servare`(지키다)이고 `serve` 는 `servire`(섬기다)다. 묶어서 설명하면 틀린 것을 가르친다.
어근을 새로 묶을 때 어원을 확인하고 넣어라. 전체 어근 표는 README 에 있다.

### 4군은 "쉬워 보여서 틀리는" 자리다

`ground`를 땅으로만, `novel`을 소설로만, `interest`를 관심으로만, `patient`를 환자로만
알고 지문에 들어가면 문장이 통째로 어긋난다. 이 부류를 일부러 모았다.

---

## 3. 구조가 지금 모양인 이유

### `createApp()` 이 listen 하지 않는다

`server/app.ts` 는 앱만 만들고, `listen` 은 `server/index.ts` 만 한다.
테스트(supertest)·로컬 개발·Vercel 서버리스가 **같은 앱**을 쓰기 위해서다.
listen 을 app.ts 로 옮기면 테스트가 포트를 물고 죽는다.

### 단어 파일이 여러 개로 갈라져 있다

`tier4.ts`, `tier4b~4f.ts`, `tier5~5e.ts` — 이름은 **군 + 저작 차수**다.
군이 다른 게 아니라 쓴 시점이 다르다. 한 파일에 300개를 넣으면 편집이 불가능해진다.
넓힐 때는 새 파일을 만들어 `shared/catalog.ts` 의 `ALL` 에 이어 붙인다.

### 카탈로그가 조용히 틀리지 않는다

`shared/catalog.ts` 가 합치면서 검사한다 — `senseKey` 가 실제 가지를 가리키는지,
`<b>` 강조가 있는지, `rank` 가 겹치지 않는지. **어긋나면 앱이 뜰 때 예외로 죽는다.**
경고만 찍고 넘어가게 바꾸지 마라. senseKey 가 어긋나면 채점 프롬프트에 빈 정답이
들어가는데, 그건 앱이 안 뜨는 것보다 나쁘다.

---

## 4. 손대면 깨지는 것들

전부 **로컬은 멀쩡한데 배포에서만 깨지는** 종류다. 세 번 넘어지고 알아냈다.

### ① `api/` 엔트리는 커밋돼 있어야 한다

Vercel 은 **저장소 소스**의 `api/` 를 보고 함수를 등록한다. gitignore 하고 빌드 중에
만들어 내면 `vercel deploy` CLI 로는 통하는데 **git 푸시 배포에서는 함수가 통째로 없다** —
모든 `/api/*` 가 플랫폼 404. 빌드는 성공하므로 로그에 단서가 없다.

→ `api/[...path].js` 는 커밋되는 한 줄 shim, 알맹이만 `dist-api/` 로 생성.
`vercel.json` 의 `functions.includeFiles` 가 번들 포함을 못 박는다.

### ② 엔트리에서 `.ts` 를 직접 import 하면 안 된다

Vercel 은 `api/*.ts` 를 트랜스파일만 하고 import 경로를 손대지 않는다. 이 저장소는
import 에 `.ts` 확장자를 쓰므로 런타임에 `ERR_MODULE_NOT_FOUND` 로 죽는다.
→ `scripts/build-api.mjs` 가 esbuild 로 미리 한 파일로 묶는다.

### ③ catch-all 은 `/api` 아래 한 단계까지만 잡는다

`/api/grade/context` 는 함수에 닿기도 전에 플랫폼 404 였다. 그래서 라우트가
`/api/grade-context` 로 평탄하다. **경로를 두 단계로 되돌리지 마라.**

> **진단법**: 그 경로에 POST 해 보고 **Express 404**(`Cannot POST …`)면 함수까지 간 것,
> **Vercel 404**(`NOT_FOUND` + 요청 ID)면 라우팅이 못 간 것이다. 이 구분이 핵심이다.

### ④ 배포 후에는 반드시 찔러 본다

**빌드 성공 ≠ 함수 살아 있음.** 위 셋 다 빌드는 통과했다.

```bash
curl https://core-meaning-lab.vercel.app/api/health   # {"ok":true,...,"words":300}
```

### ⑤ 이 PC 의 포트 충돌 (환경 문제)

5173/5174 를 다른 프로젝트가 쓰고 있었다. 먼저 뜬 쪽이 `::1` 에 바인드하면
`localhost` 요청이 통째로 그쪽으로 간다 — 우리 서버는 "5174 에서 대기 중"이라 찍는데
응답은 남의 앱이 준다. 알아채기 고약하다.
→ `CLIENT_PORT=5183 PORT=5184 npm run dev`. Vite 는 IPv6 로만 뜨므로 `http://[::1]:5183`.

### ⑥ 한글 답안 테스트는 인코딩을 챙겨라

Git Bash `curl` 로 한국어를 보내면 깨져서 채점기가 "글자가 깨져 있다"고 답한다.
**앱 버그가 아니다.** PowerShell 에서 UTF-8 바이트로 보내면 정상이다:

```powershell
$b = [System.Text.Encoding]::UTF8.GetBytes('{"wordId":"run","taskIndex":0,"answer":"운영하다"}')
Invoke-WebRequest $url -Method POST -ContentType "application/json; charset=utf-8" -Body $b
```

---

## 5. 검증된 것 / 검증 안 된 것

정직하게 갈라 둔다. 여기를 믿고 다음 사람이 시간을 아끼면 된다.

**검증됨**

- 자동 검사 53개 — 카탈로그 무결성, 오프라인 폴백, API 계약, 진도 계산,
  그리고 **300단어 전 문맥과제 600건 + 역방향 300건이 400 없이 채점되는지 전수 확인**.
- 헤드리스 크로미움으로 5단계 한 바퀴(제시→의미확장→문맥복원→역방향→리포트),
  티어 탭 전환, 다크모드. 콘솔 에러 0건.
- 프로덕션 엔드포인트 4종 실호출, 실제 Gemini 채점(정답/오답 각각), 잘못된 입력 → 400.
- 클라이언트 번들에 API 키가 실리지 않음(배포된 JS 전수 검색).
- public 전환 전 git 히스토리 전체 시크릿 스캔(.env 커밋 이력·키 패턴) — 깨끗.

**검증 안 됨 — 여기가 진짜 남은 일이다**

- ⛔ **실제 학습자가 이 앱을 써 본 적이 없다.** 특히 라틴계 어원 설명(3·5군)이
  고등학생에게 통하는지 모른다. 여기서 막히면 고칠 것은 기능이 아니라 **데이터**다.
- ⛔ **`bridge` 900개의 품질을 사람이 전수 검토하지 않았다.** 형식은 검사가 보증하지만
  "이 다리가 실제로 납득되는가"는 사람만 판단할 수 있다.
- ⛔ **저빈도 18개를 교체했지만, 여전히 18개는 565 정본에 한 번도 안 나온다.** 어근 가족을
  4개로 지키려고 남긴 것이다(persist·inspect·admit 등). 학습 장치 > 빈도로 판단했으나
  검증된 선택은 아니다.
- ⛔ 여러 기기에서 이어 학습해 본 적 없다(진도가 `localStorage` 라 애초에 안 된다).
- ⛔ 부하·비용을 재 본 적 없다. 아래 §7 참고.

---

## 6. 단어를 더 쓸 때

`shared/data/` 의 4·5군 파일에 이어 붙인다. 1~3군은 늘리지 않는다.
형식은 README 에 있고, 여기에는 **형식이 아니라 결의 문제**만 적는다.

- `core.ko` 는 **한 줄로 그림이 그려지는 말**이어야 한다. "다루다"보다 "손으로 다루다".
- 라틴계면 `core.image` 끝에 어원을 괄호로 — `(sub + tenere, 아래에서 붙들다)`.
  **어원은 확인하고 쓴다.** 그럴듯한 추측을 적으면 틀린 것을 가르치게 된다.
- `bridge` 는 "왜 그 뜻이 되는가"를 한 문장으로. 여기가 이 앱의 전부다(§1).
- `context` 문장은 **수능 지문의 결**로. 추상어일수록 논설문 문장이 낫다.
- 5군은 어근 가족으로 묶어 네 개씩. 낱개로 넣으면 이 방식의 값이 안 산다.

새 군을 만들 때는 `shared/types.ts` 의 `Tier` 와 `TIERS` 만 고치면 탭·리포트·검사가
전부 따라온다 (`GroupKey` 도 `Tier` 에서 파생된다).

---

## 7. 열린 판단 — 사람이 정할 몫

1. **rate limit.** 저장소가 public 이고 `/api/grade-*` 는 인증 없이 열려 있다.
   URL 이 퍼지면 Gemini 할당량이 그대로 깎인다. 지금은 아무 제한이 없다 —
   의도한 게 아니라 아직 안 한 것이다.
2. **Gemini 키.** Production·Preview·Development 에 등록돼 있다. 사용량이 늘면
   전용 키로 가르는 것을 검토한다.
3. **진도 저장.** `localStorage` 라 기기 간 공유가 안 된다. 여러 기기 학습을 지원하려면
   서버 DB 가 필요하고, 그러면 사용자 식별이 따라온다.
4. **4군의 처지.** 프로토타입 유산 20개(`break` `light` `set` `line` …)가 섞여 있다.
   수능 우선순위 목록에서 나온 게 아니라 출발점에서 딸려 온 것이다. 정식 편입할지,
   빼고 다른 것으로 채울지 정하지 않았다.

---

## 8. 이어받는 순서

```bash
git clone https://github.com/smilepat/core-meaning-lab.git
cd core-meaning-lab && npm install
vercel env pull .env.local     # 또는 .env 에 GEMINI_API_KEY 직접 기입
npm run check                  # typecheck + test 53 + build — 여기가 통과해야 시작이다
npm run dev                    # 포트가 겹치면 §4-⑤
```

읽는 순서: `STATUS.md`(지금 뭐 하던 중) → 이 문서(왜 이 모양인가) →
`README.md`(실행·구조) → `CLAUDE.md`(저장소 규칙).
