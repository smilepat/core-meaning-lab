---
project: core-meaning-lab
status: active
progress: 95
updated: 2026-08-14
pc: DESKTOP-JDF6C5D
---

# core-meaning-lab — STATUS

## 🎯 한 줄 상태

**Core Meaning 300 완주 · 코드는 끝났다.** 300단어(20/20/20/133/107) · 900 의미가지 ·
600 문맥과제 · 300 역방향과제, 테스트 **75개**, `main` 푸시 자동 배포, 실제 Gemini 채점 —
<https://core-meaning-lab.vercel.app>. 저장소는 **public**.
단어는 수능 565 정본 기출 지문 빈도에 대조해 한 번 걸렀다.

**남은 것은 기능이 아니라 검증이다 — 실제 학습자가 아직 한 명도 써 보지 않았다.**
`bridge` 품질·어원 설명이 통하는지·0회 단어 18개를 유지할지, 전부 여기서 답이 나온다.

## 📊 진행 체크리스트

- [x] 저장소 생성 · 스택 결정 (Vite + React + TS + Express)
- [x] 프로토타입 데이터 추출 (30단어)
- [x] 프로토타입 CSS 이식 (다크모드·모바일 대응 그대로)
- [x] 5단계 화면 React 이식 (제시 / 의미확장지도 / 문맥복원 / 역방향생성 / 리포트)
- [x] Express 채점 백엔드 — 구조화 출력 + 오프라인 폴백
- [x] 채점 공급자를 Anthropic → Gemini(`gemini-3.6-flash`)로 교체
- [x] API 키 서버 격리 · 프롬프트 주입 차단(서버가 문장을 조회)
- [x] 실제 API 키로 채점 동작 확인 (문맥·역방향 각각 정답/오답 케이스)
- [x] **수능 우선순위 체계 도입** — 1군 20 / 2군 20 / 3군 20 / 확장 20, 최우선 30개 순위
- [x] **단어 30 → 80 확장**, `core30.ts` 졸업(삭제) → `shared/data/tier1~4.ts` 가 정본
- [x] **티어 탭 UI** — 80단어를 그룹으로 나눠 고르고, 리포트에 그룹별 진도 막대
- [x] 테스트 49개 (카탈로그 무결성 · 오프라인 폴백 · API 계약 · 진도 계산)
- [x] **Vercel 배포** — 정적 dist/ + 서버리스 함수 1개, 엔드포인트 4종 실동작 확인
- [x] **브라우저에서 5단계 흐름 실제 확인** — 헤드리스 크로미움으로 제시→의미확장→문맥복원→
      역방향→리포트 한 바퀴 + 티어 탭 전환 + 다크모드, 콘솔 에러 0건
- [x] **PR #1 병합 → `main` 일원화**, 브랜치 정리
- [x] **git 푸시 자동 배포 성립** — `api/` 엔트리를 커밋되는 shim 으로 바꿔 해결.
      (전에는 CLI 배포만 통하고 git 배포는 함수가 통째로 없었다. README "세 번 넘어졌다" 참고)
- [x] **`GEMINI_API_KEY` 등록 → 실제 AI 채점 동작** (Production·Preview·Development 3개 환경).
      Preview 는 CLI 가 프롬프트를 요구해 실패 → REST API 로 우회 (전역 규칙대로).
      로컬·프로덕션에서 정답/오답 각각 확인, 클라이언트 번들에 키 없음도 확인.
- [x] **저장소 public 전환** — 전환 전 히스토리 전체 시크릿 스캔(.env 커밋 이력·키 패턴) 통과
- [x] **80 → 120 확장** — 4군 +20(`last` `matter` `count` `issue` `stick` …),
      5군 신설 +20(`attribute` `impose` `expose` `subject` `sustain` …).
- [x] **120 → 160 확장** — 4군 +20(`case` `ground` `field` `yield` `bound` `tell` …),
      5군 +20을 **어근 가족**으로 묶어 저작: sistere(서다) 4형제 · tenere(붙들다) 4형제 ·
      ducere · premere · servare · vertere 계열. README 에 뿌리별 표.
      1~3군 정본 60개는 계속 손대지 않았고, 테스트가 각 20개로 못 박는다.
- [x] **160 → 200 확장** — 5군 +20을 **다섯 어근 × 네 형제**로 대칭 저작
      (ferre · capere · spectare · mittere · tendere), 4군 +20은 "쉬운 뜻이 먼저 튀어나와
      지문을 망치는" 부류(`novel` `article` `patient` `just` `plain` …).
- [x] **200 → 240 확장** — 5군 +20을 또 다섯 어근 × 네 형제로
      (ponere · cedere · struere · plicare · vocare), 4군 +20은 "명사로만 외운 단어가
      동사로 나오는" 자리(`book` `stress` `second` `spare` `mine` …).
      `succeed` 가 이 방식의 값을 가장 잘 보여 준다 — core "뒤따라 가다" 하나에서
      성공하다·계승하다가 함께 나온다.
- [x] **240 → 300 확장 · 완주** — 5군 +32를 여덟 어근 × 네 형제로
      (trahere · scribere · portare · currere · venire · stare · fundere · gradi),
      4군 +28은 "추상 지문에서 갑자기 다른 얼굴로 나오는 쉬운 단어"
      (`interest` `nature` `party` `means` `stake` …).
      `circumstance` = 둘레에 서 있는 것들 — 번역어 "상황"보다 정확하다.
- [x] 테스트에 **역방향 과제 300건 전수 검사** 추가. 문맥 600건 검사는 순차 호출로
      5초 타임아웃에 걸려 병렬로 바꿨다 (실패가 아니라 느림이었다).
- [x] **앱 안 안내 화면** — 목적·원리·학습 체계·사용법. 첫 방문 자동 표시,
      헤더 '안내' 버튼으로 재열기. 숫자는 카탈로그에서 뽑으므로 낡지 않는다.
- [x] **HANDOFF.md** — 결정의 이유 · 배포 함정 · 검증된 것/안 된 것
- [x] **자가 진단 → 학습 결함 3종 수정** — 오프라인 채점이 굴절형을 못 알아봐
      **모범답안 7개가 0점**이었고(→ 0/300), 한 글자 gloss 가 걸러져 정답이 0점이었으며
      (→ 0/600), 19개 단어가 확장 의미 3개 중 1개만 연습시켰다(→ 0개).
      `infer` 의 뜻 하나가 `imply` 것으로 잘못 들어가 있던 것도 함께 고쳤다.
- [x] **300개를 찾는 길 세 개** — 검색(전체) · 5군 어근 29가족 · 4군 알파벳 30개 단계.
      4군 25화면 → 5.9화면, 5군 27화면 → 1화면.
- [x] **리포트 "다음 단어" 카드** — 안 한 것 먼저, 다 했으면 약한 것부터
- [x] **말뭉치 대조 → 저빈도 18개 교체** — 원래 계획의 "말뭉치 분석"을 뒤늦게 이행했다.
      `csat-vocab-coverage` 레포에 **수능 565 정본 기출 지문**이 있어 COCA 가 필요 없었다.
      어근 대칭을 맞추려고 넣은 5군 단어가 지문에 거의 안 나오고, `make`(230회)
      `use`(222회) 같은 최고빈도 다의어가 빠져 있었다 → 18개 교체.
      결과: 0회 단어 28 → 18, 6회 이상 203 → 221.
- [x] **단계 바를 내용 가까이** — `nav` 를 fixed → sticky 로 바꾸고 `main` 의
      `min-height:60vh` 를 뺐다. 데스크톱에서 카드와 단계 바 사이 여백이 **0px**.
      모든 단계에 다음으로 넘어가는 버튼도 넣었다(전에는 STEP 1 에만 있었다).
- [x] **제호(Masthead)** — 페이지 맨 위(sticky 헤더보다 위)에 앱 이름과 주장을 둔다.
      "다의어는 사전 뜻을 여러 개 외워서 풀리지 않는다 · 공통 개념 하나를 잡아야 한다"
      + 한 줄 그림 `~~뜻을 하나씩 외우기~~ → core 하나로 꿰기`.
      안내 화면은 한 번 닫으면 다시 안 뜨므로 전제가 매번 보이는 자리에도 있어야 한다.
- [ ] **실제 학습자 검증**  ← 현재 위치 · 코드로 더 할 일이 없는 지점

## ⏭️ 다음에 할 일 (Next Actions)

1. **학생 한 명, 10분.** `run`(1군) → `ground`(4군) → `circumstance`(5군).
   군마다 성격이 달라서 세 개면 충분하다.
   **볼 것은 딱 하나 — STEP 2 에서 🔗 한 줄(bridge)을 읽고 "아" 하는지, 그냥 넘기는지.**
   넘긴다면 고칠 것은 기능이 아니라 데이터다.
2. 그 결과로만 정해지는 것들: `bridge` 표현을 손볼지 · 3·5군 어원 설명이 고등학생에게
   통하는지 · 565 정본에 0회인 18개를 유지할지.
3. **단어 확장은 보류.** 넓힐 여지는 있다(구동사 `run into`/`stand for`/`put off`,
   `flectere`·`ligare`·`tangere` 가족). 다만 300 이 통하는지 모르는 채로 340 을 만드는 건
   같은 실수를 키우는 일이다. 넓힐 때는 4·5군 파일에 붙이고 1~3군은 늘리지 않는다.
4. **rate limit 도 보류.** 서버리스 메모리 제한은 인스턴스마다 따로 놀아 실효가 없고,
   제대로 하려면 KV/Redis 가 필요하다. 사용자 0명에 인프라를 붙이는 건 이르다.
   먼저 할 일은 **Gemini 키를 restoration-reader 와 분리**하는 것이다(코드 0줄).
5. 새 PC 에서 이어받을 때: `git clone` → `npm install` → `vercel env pull .env.local`
   (또는 `.env` 에 `GEMINI_API_KEY` 직접 기입) → `npm run dev`.

> 이 PC(DESKTOP-JDF6C5D)에서는 다른 프로젝트가 5173/5174 를 이미 쓰고 있었다.
> 먼저 뜬 쪽이 `::1` 에 바인드하면 `localhost` 요청이 통째로 그쪽으로 가서
> "우리 서버는 떴다는데 응답은 남의 앱"이 된다. 겹치면 포트를 바꿔 띄운다:
> `CLIENT_PORT=5183 PORT=5184 npm run dev`

## 🤔 결정 대기 (Decisions Needed)

- ⚠️ **Gemini 키 분리** (가장 먼저 할 일, 코드 0줄): 지금 키는 `restoration-reader` 와
  공유다. 이 앱에서 무슨 일이 생기면 **다른 프로젝트가 같이 죽는다.** 저장소가 public 이라
  URL 이 퍼지면 호출이 늘 수 있으므로 폭발 반경부터 끊는 게 순서다.
  rate limit 은 그 다음 — 트래픽이 생긴 뒤에 붙인다.
- **이 앱을 누가 쓰나**: 선생님 검증용인지, 학생들에게 배포할 것인지에 따라 아래 둘이
  한꺼번에 정해진다 — 진도 저장(브라우저 vs 서버)과 rate limit 필요 여부.
- **진도 저장 위치**: 지금은 브라우저 `localStorage`. 사용 범위를 **선생님 검증용·학생 1~2명**
  으로 정했으므로(2026-08-14) 이대로 둔다. 한 기기를 돌려 쓰는 문제는 리포트 맨 아래
  **"처음부터 다시 시작"** 으로 해결했다. 교실 배포로 넓힐 때 서버 DB 를 다시 검토한다.
- **565 정본에 0회인 18개**: `persist` `inspect` `admit` `evoke` … 전부 5군이고,
  어근 가족을 4형제로 지키려고 일부러 남긴 것이다. 빈도보다 학습 장치를 택한 자리인데
  **검증된 선택은 아니다**(HANDOFF §5).
- **300개 확장 방식**: 지금은 단어마다 사람이 쓴다(core·bridge·예문 5종). 품질은 좋지만 느리다.
  LLM 초안 → 사람 검수로 갈지는 확장을 재개할 때 정한다.

### 해결된 것

- ~~**4군 유산 20개의 처지**~~ — 데이터로 답이 나왔다(2026-08-14). 프로토타입에서 딸려 온
  `head` `break` `light` `get` … 20개를 수능 565 정본에 대조하니 **0회가 하나도 없고
  17개가 6회 이상**이다: `get` 97 · `hand` 77 · `play` 69 · `set` 53 · `stand` 40.
  우선순위 목록에서 나오지 않았을 뿐 제 값을 한다 → **전부 유지.**
  낮은 셋은 `spring` 2 · `drop` 4 · `bright` 5 인데, 다의어로서는 값이 있어 남긴다.

## 🔗 Claude Code 재개 프롬프트

"STATUS.md 읽고 core-meaning-lab 이어서 하자"
